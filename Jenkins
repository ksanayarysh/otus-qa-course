pipeline {
	agent {
		docker {image "docker-otus"}
	}
	stages {
        	stage('Build') {
            		steps {
                		sh "pytest Lesson6/selenium -q --address=" + params['address']
            		}
        	}
        	stage('Test') {
            		steps {
                		echo 'Testing..'
            		}
        	}
        	stage('Deploy') {
            		steps {
                		echo 'Deploying....'
            		}
        	}
    	}
}
